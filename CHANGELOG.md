# 更新日志

本文件记录 Dynamic Panel 的重要功能、修复与发布变更。尚未发布的改动统一进入「未发布」，创建新版本标签时再归档到对应版本。

## [未发布]

### 桌面

- 修复 macOS 面板失焦收起时旧展开区域短暂残留矩形框的问题；透明主窗口改用已有 CSS 面板阴影，不再保留覆盖整个窗口边界的系统原生阴影。新增独立真实焦点转移回归命令 `npm run test:panel-blur`。
- 以原始项目节奏为基础加快 macOS 收起：失焦后立即退场，外壳用 `220ms` 收拢并在末段用 `80ms` 快速淡出；Windows 继续保留 `80ms` 焦点防抖，避免原生控件短暂失焦时误收起。

## [1.2.1] - 2026-09-24

### 桌面

- 修复 macOS 面板失焦收起时外壳过早消失、短暂露出白色窗口底色的问题；外壳现在会随收起轮廓同步退场。
- 修复 macOS 文本输入时输入法候选窗口可能被主面板遮挡的问题；仅在文本控件获得焦点期间降低窗口层级，收起后恢复原有置顶行为。
- 修复折叠刘海跨显示器移动后抓握横杠偶尔消失的问题；重定位时同步目标显示器的菜单栏高度，并让折叠高度立即匹配原生窗口边界。

## [1.2.0] - 2026-09-23

### 桌面

- 新增可选的自托管 Sync MVP：桌面端按工作区保存独立的安全绑定、持久 outbox/inbox、游标、冲突与对象传输状态，支持 11 类 allowlist 数据、离线重试、实时失效通知、ACK 后推进游标、笔记/剪贴板/截图 PNG 续传、冲突详情与手动解决，以及带加密服务端恢复点、签名影响计划、本地恢复清单和精确确认的首次同步 `local-wins` / `server-wins`。新增独立 Node.js 22 同步服务、PostgreSQL RLS、多账号/空间/客户端 Key、严格 CSP Web 控制台、管理员 CLI、S3/文件系统对象存储、加密备份/暂存恢复、Docker/Compose 示例和真实双客户端环回验收；默认仍纯本地，剪贴板、截图、AI 会话、行情、指令、启动器与地点默认不同步。

- 修复同步设置区在设置页面重组时被移出 DOM，以及加入导航后稳定 ID 被改写的问题；新增真实 Electron 同步 runtime、导航、首次同步影响和冲突界面验收。收藏剪贴板条目不再被 100 条普通滚动历史自动淘汰。

- 新增截图标注编辑：区域框选后可添加矩形、箭头、画笔、文字和马赛克，支持颜色、粗细、撤销与重做；截图资料库中的已有 PNG 也可再次编辑，保存时保留原条目并复制到系统剪贴板。

- 修复 Windows 录屏选择来源后无法进入区域框选的问题：采集专用 session 现在只在已选择来源且处于准备阶段时放行 Chromium 的桌面视频权限探测，普通摄像头请求仍保持拒绝；停止录屏前主动冲刷编码数据。

- 新增录屏区域与状态提示：确认区域后立即显示鼠标穿透的红色范围边框和受保护控制条，覆盖准备、倒计时、录制、停止与保存状态，可直接停止并保存或取消并丢弃；面板和托盘同步区分保存与丢弃操作。

- 修复区域录屏冻结画面仍可能残留主面板或来源选择器的问题：采集窗口启用内容保护，隐藏主面板和选择器后等待桌面合成帧排空，再显示区域框选画面。

- 新增统一的全局快捷键设置：可分别配置面板展开/收起、搜索启动器、区域截图、区域录屏和录音开始/停止；截图、录屏与录音默认不占用组合键，可随时禁用。区域录屏快捷键确认屏幕来源后直接进入区域框选，不修改默认录屏方式。应用内动作会检测彼此及系统快捷键冲突，注册或保存失败时恢复原设置。

- 修复 Electron 44 下截图自动复制和资料库「复制图片」无效的问题：改用异步 PNG `ClipboardItem` 写入系统剪贴板，并等待写入完成。

- 启动器搜索支持中文标题、别名、副标题和关键词的拼音全拼与首字母匹配，并保留原有中文原文、英文子序列和正则搜索。

- 修复展开面板内切换 Tab、点击控件或操作原生下拉框时偶发误收起的问题：主窗口 blur 后先等待焦点稳定，再核对窗口是否仍处于失焦状态；焦点恢复及受保护的原生操作不会触发收起。

- 修复顶部折叠刘海跨显示器跟随鼠标时短暂偏离中心并闪烁的问题：可见折叠态跨屏时先隐藏原生窗口画面，待目标显示器的 bounds 与 shape 稳定后再显示；隐藏、关闭或展开会取消未完成的重定位。

- 修复 Windows 采集来源列表批量抓取窗口缩略图触发 WGC `CreateForWindow / Source is not capturable` 的路径：窗口列表改为只枚举名称，过滤自身、最小化、隐藏、已销毁或明确禁止采集的窗口；选择和开始前重新验证来源，捕获启动失败后留在选择器提示重选，不自动改录其他屏幕。

- 新增截图与录屏工作区：截图点击后直接在鼠标所在屏幕显示全屏覆盖层，拖动框选并在松开时自动复制到剪贴板，同时保存 PNG；Windows 静态截图使用 GDI 后备路径，避免部分显卡、远程桌面和混合 DPI 环境下 Chromium DXGI 抓屏失败。录屏支持整屏、窗口和单屏区域采集及主动麦克风。采集窗口、权限、托盘停止、分块落盘、未完成文件保留、Range 预览、导出、复制、重命名、删除和工作区迁移均由主进程统一管理。暂不启用系统声音或 MP4 转码。
- 录屏区域支持拖动框选、精确输入位置和宽高、本机记忆与清除；显示器、缩放或画面尺寸改变后须重新选择，区域录屏按裁剪后尺寸编码，录制中尺寸变化自动停止。

- 扩展美股数据源：新增默认关闭的 Twelve Data 报价、名称搜索与 1D/7D/30D 历史 adapter，每轮最多处理 8 个未由 Alpaca 解析的代码，并明确 Basic feed 不是全市场 SIP、`market_movers` 需要 Pro；新增 SEC EDGAR 官方 XBRL 基本面详情，使用安全保存的 Fair Access 联系邮箱并逐项显示申报表单、期间和披露日，不把申报值当作实时估值。Alpaca 设置明确 IEX 是单一交易所 feed；Alpha Vantage 免费榜单明确为日终数据并使用不可被手动刷新绕过的 12 小时内存缓存。新增安全设置、固定 origin、受控 IPC、能力测试和 Electron 详情验收。
- 修复应用启动后行情必须打开页面才开始获取的问题：启动阶段由主进程执行一次受限预热，将总览、当前榜单和自选资产放入进程内存缓存；隐藏页面不启动周期刷新，打开行情页时先展示预热结果再按设置更新。
- 接入腾讯 `qt.gtimg.cn`、东方财富 `push2` 和新浪 `hq.sinajs.cn` 三个可分别启停的公开 A 股 provider：腾讯/新浪解析 GBK 实时批量快照，东方财富解析 JSON 列表、榜单分页、按代码报价和日 K；公开接口按 QuantDash → 腾讯 → 东方财富 → 新浪优先回退、按证券代码去重，失败时显示明确 provider 状态，不承诺稳定 SLA 或显示/再分发授权。
- 优化金融页 AI 解读：新增专用结构化 `financeInterpretation` 动作，按快照时点输出市场语气、简短结论、正向/负向/中性信号和后续观察项；每条信号必须引用快照中的连续证据行，超出条数或无法核对的返回会被拒绝。再次生成、生成失败或行情更新时会在当前窗口保留上一份解读并标明状态，只有新解读成功后才替换；解读仍不持久化，模型不联网、不预测价格、不输出买卖或仓位建议；新增 GitHub 金融分析项目研究记录。
- 重构与首页平级的金融观察页（开发中）：移除旧三栏结构、内置 fixture、随机价格漂移和伪指数/市场宽度/板块/走势图，改为全市场快照、市场宽度、真实历史曲线、完整榜单和按需自选详情三个清晰工作区。榜单将 Binance 作为加密货币市场内的可选数据源，支持从 CoinGecko 聚合与 Binance USDT 现货之间切换；榜单前三和表格行均可直接打开当前项目详情，未加入自选的项目仅作为临时详情展示；榜单按主进程排序结果分页，每页最多 50 行，页码纳入缓存键和后台刷新活动但不写入用户偏好。CoinGecko 公共 REST 提供真实的加密市场总值与聚合榜单，Binance Public REST 提供明确标注的 USDT 现货榜单/报价/历史，Alpha Vantage 使用用户 API Key 提供美股涨跌和成交榜，Alpaca 支持用户配置 IEX / SIP Key/Secret 后读取美股搜索与批量快照；A 股保留用户 API Key 驱动的 QuantDash adapter，并新增可分别启停的腾讯、东方财富、新浪公开行情 adapter：QuantDash 通过 `X-API-Key` 读取约 3 秒刷新的实时快照、批量自选报价、精确代码搜索和日 K 历史，Free 实时快照每分钟 10 次、每次最多 5 只，日 K 每分钟 10 次、每次仅 1 只；东方财富公开接口提供 A 股列表、榜单分页、按代码报价和日 K，腾讯与新浪提供按代码批量实时快照。全市场榜单使用有权限的 QuantDash `CN_Stock` 标的池或已启用的东方财富 provider 原生排序页；QuantDash Free 单独使用时仍不能生成完整 A 股排名。名称搜索只使用 QuantDash 已取得的目录或进程内已加载的东方财富榜单页；因响应没有全市场市值字段，A 股市值榜明确降级为不支持。旧 `cn:tushare:*` 自选 ID 继续兼容读取，但旧 Token 不迁移为 QuantDash API Key。账户计划支持时读取全球总市值历史曲线，否则明确显示真实 BTC 市场代理；资产详情支持 1D/7D/30D 历史走势。定时预取移至 Electron 主进程并遵循设置刷新频率与页面可见状态；页面点击先保留现有快照，后台成功后再替换。新增用户主动触发的 AI 市场解读，只将当前 provider 快照交给既有 AI 网关，结果不构成投资建议。凭据使用 `safeStorage` 或环境变量，网络请求限定公开 HTTPS provider 主机并执行 DNS 固定、超时、响应上限和拒绝重定向。行情页保持只读，设置页负责 provider 保存/清除/连接测试、自选管理及展示偏好；QuantDash API Key 仍须由用户确认套餐权限与展示用途；密钥不得共享或转让，未经授权不得提供数据转售服务。升级会剥离旧自选行情字段并移除未经用户确认的默认 fixture。
- 音乐模块新增来源与歌单切换：保留本地文件和公开 HTTPS 直链，并可连接本机 go-music-dl 服务，读取其已收藏或已导入的多平台歌单；首页与设置页均可切换歌单，播放控制和自动续播限定在当前队列。首页音乐卡片新增在线发现入口，可直接选择聚合源、平台、分类或搜索并播放在线歌单，当前歌曲和歌单可通过受控封面代理显示图片。设置页将“我的音乐”和“在线发现”提升为模块级工作模式，来源管理、当前队列和在线浏览分区展示；分类歌单结果区扩大为独立滚动区域，避免只能看到少量结果；首页在线发现改为可展开的覆盖式歌单浏览面板，首页音乐头部只显示当前播放歌单和队列数量，歌单切换集中在设置页与在线发现；首页和设置页在线发现会在首次进入时自动加载平台分类，并记住来源对应的平台分类，刷新后保持当前筛选；点击歌单后会自动加载第一首并开始播放。音乐库自动迁移到 schema v2，聚合服务仅允许回环 HTTP，并限制来源、歌单、曲目、JSON 响应和音频大小。
- 重设计首页音乐播放器：使用更清晰的来源与队列信息、唱片封面、进度时间和底栏播放控制，并提供常驻音乐库入口；本地与网络音乐使用不同状态色，播放动画支持减少动态效果。聚合源设置进一步拆分为“我的歌单 / 在线发现”，当前队列、来源管理和在线浏览互不挤压。
- 修复音乐播放时切换本地/网络来源面板或切换曲目时的闪烁和中断：设置浏览来源与实际播放队列解耦，固定来源配置区及播放进度区高度，并在首页离场时暂停装饰动画。聚合曲目封面经 go-music-dl 安全代理读取为受限图片字节，渲染层使用 Blob URL 展示，避免 CSP 阻断和外链直读。
- 首页改为个人工作台：今日到期事项、最近笔记、独立快速收集、手动城市天气、应用内音乐播放器和 AI 对话；移除「经典工具」及其设置入口，但不清理旧速记、排列或显隐偏好数据。天气使用 Open-Meteo 非商业免费接口：首页突出当前天气、体感、未来 6 小时最高降雨概率、湿度和风，详情提供未来 12 小时与 7 天趋势，同时保留缓存和错误状态。音乐不再依赖 Windows SMTC 或 macOS Music/Spotify，可在设置中添加本地音频文件、递归导入音乐文件夹或添加公开 HTTPS 音频直链，并在首页显示来源、曲目、只读进度及应用内播放控制。
- 重构首页 AI 对话为临时工作会话：显示当前厂商与模型，增加工作型起始建议、安全 Markdown/代码块、自动增高输入框、新建对话，以及回复复制、重试、保存笔记与撤销操作。停止或失败后保留当前轮次和恢复入口，但不把不完整结果写入后续上下文；模型生成外链统一经过公网 DNS 校验。
- AI 临时对话支持为单条消息显式选择最多 3 份笔记、录音、待办、链接或文字剪贴板资料；输入区显示可移除标签，用户消息保留来源标识，发送和重试使用冻结快照。资料按不可信 JSON 参考数据传给模型，并与问题及历史共同遵守 12,000 字符限制；图片剪贴板、未选择内容和超限资料不会发送。
- AI 对话新增当前工作区会话历史：默认仍为不持久化的临时对话，用户确认保存范围后才复制消息、回答和已选资料的冻结文字快照；支持自动命名、搜索、切换、行内重命名、二次确认删除、刷新恢复及失败/停止轮次重试。最多保存 30 个会话，单会话 512,000 字符、全部会话 2,000,000 字符，超限明确拒绝且不自动淘汰旧数据。
- AI 完整长回答可打开独立阅读工作台，提供安全 Markdown、标题大纲、全文/选区/代码复制、保存笔记与撤销，以及复用现有预览流程的待办提取；全文超过 12,000 字符时要求用户先选择合规范围，停止、失败或生成中的内容不会开放入口。
- 修复长回答工作台的原生 Escape、非模态语义、面板收起生命周期、代码围栏识别和 AI 待办预览焦点恢复；补充真实键盘、剪贴板、全文/选区待办及保存撤销验收。
- 新增显式触发的多轮 AI 对话，复用内容模型与安全网关；最多最近 6 轮、总输入 12,000 字符，默认仅保留会话内存。修复从首页或启动器打开笔记时被旧筛选条件隐藏的问题。
- 修复笔记输入时自动保存状态长短变化导致的编辑区上下跳动：保存提示使用独立固定高度行，输入与保存完成时保持编辑器位置、焦点和光标稳定。
- 提高各面板文字可读性：辅助文字统一至少 12px，常用控件 14px、正文 15px；将首页、待办、笔记、链接、录音、密钥、设置及启动器中的小字号接入共享字体变量，提亮次要文字和输入占位提示，并调整顶栏搜索按钮避免换行。
- 重构笔记工作区：宽窗口采用“分类与标签树 / 笔记列表 / 编辑器”三栏结构，窄窗口将分类树与列表放在左侧上下分区；分类树支持折叠、数量、键盘导航和聚焦时行内管理，搜索、列表与编辑器保持独立滚动。
- 笔记新增“分类 → 标签”两级整理：分类及其标签均可创建、重命名和删除，可与正文关键词组合筛选，并在详情中调整归属。标签限定在所属分类内，切换分类会清除不兼容标签；删除标签只移至“无标签”，删除分类只移至“未分类”，均不删除正文或图片。旧 `notch-note-archive-v1` 和 `notch-note-categories-v1` 数据保持兼容。
- 新增统一 AI 整理工作区：支持文字摘要、精简、翻译和待办提取，笔记选区可预览替换，录音转写可生成摘要与行动项；生成结果均需用户确认后写入，并提供本次会话内的冲突安全撤销。
- AI 设置拆分为实时转写和内容整理两个独立服务槽位；内容服务新增 DeepSeek、OpenAI、阿里云百炼、Kimi、智谱 GLM、SiliconFlow、Gemini、OpenRouter、Azure OpenAI、Anthropic 和自定义 OpenAI-compatible 厂商注册表，Anthropic 使用独立 Messages adapter。
- 重构 AI 设置交互：移除二级配置弹窗，在设置页直接以左侧服务/厂商列表和右侧表单完成选择、配置、验证、移除与保存；录音配置入口直接定位实时转写，厂商和表单状态支持键盘导航及真实未保存差异提示。
- AI 配置升级为版本 3 并幂等迁移旧字段；每个内容厂商可独立保存端点、超时、加密凭据、最多 12 个模型及默认模型，切换厂商不会丢失其他厂商配置，验证状态绑定具体模型。旧单模型配置自动成为对应厂商的首个默认模型；已存 Key 不回显，配置写入使用临时文件原子替换，移除或更换转写配置会结束仍使用旧配置的会话。
- 内容整理服务增加从 DNS 校验阶段生效的取消/总超时、固定公网解析、并发边界、输入/输出/任务数量限制和原文依据校验；设置页新增超时、文本与结构化连接测试及笔记、录音、链接独立自动命名开关，新安装默认不自动调用 LLM。
- 修复 AI 来源首尾空白误报、生成期间旧输入结果、工作区同步失败仍报保存成功及录音决定重复显示；待办草稿增加参考时间、快捷截止日期和相似/批内重复提示。
- 内容整理增加 SSE 增量事件、最近 50 条脱敏诊断及有界自动命名队列；旧配置显示一次迁移说明，笔记、录音和链接支持生成名称/分类、编辑预览、显式采用与冲突安全撤销。
- 修复 SSE 断流、畸形帧或 token 截断仍可保存部分结果、流式编辑被覆盖、首尾空白选区无法替换，以及自动请求在工作区或 AI 配置变化后继续发送的问题；排队等待现限制为 30 秒并绑定上下文代次。
- 修复取消请求过早释放并发槽位及不同笔记/录音自动命名被错误合并；排队满、过期和上下文失效现写入不含正文的诊断。
- 修复 AI 结果连续保存产生重复笔记、保存过程中关闭导致状态异常、生成中关闭重开后控件卡死，以及生成期间切换动作造成结果类型错配。
- 修复 Windows 启动器因用户开始菜单、全局开始菜单和桌面存在多个快捷方式而重复显示同一应用；相同目标与参数只保留第一条，不合并不同启动参数的快捷方式。
- 将搜索与启动器配置整合到面板设置分类，统一快捷键、来源、收藏/别名和扩展管理；搜索页管理入口跳转到面板设置。
- 链接页增加标题/网址/分组搜索、分组筛选、数量提示及全部折叠；大分组按 40 条逐批显示，各分组独立滚动并阻止滚动串到外层，保留刷新后的组内滚动位置；修复筛选时多选与目标定位。
- 修复密钥表单在矮窗口中的保存控件裁切，补齐录制详情滚动、笔记动作栏与链接/剪贴板工具栏换行，改善窄屏列表布局。
- 设置页改为通用、显示功能、首页组件、AI 与转写四类导航；内容使用单一滚动区，修复固定卡片高度和 API 配置弹窗裁切，拆分面板与启动器快捷设置。
- 启动器新增显式正则搜索与 Worker 超时保护；应用结果支持管理员启动、新窗口和已有窗口切换快捷键及动作菜单。
- 新增贴顶启动器：独立快捷键、应用和工作区搜索、键盘操作、动作面板、收藏、别名和最近使用。
- 修复顶栏搜索按钮与导航重叠；优化搜索框、彩色分类图标和 Windows 应用图标；搜索保留匹配结果并按 ID 更新节点，避免清空重建造成闪烁。
- 补齐启动器来源分组、系统应用图标、右键动作、本机绝对路径打开、集中收藏/别名管理和减少动态效果支持。
- 扩展增加文件/工作区导航动作、入口和权限展示、持久化运行历史及可配置查询超时；补充真实 Electron 执行与系统剪贴板验证。
- 新增本地扩展目录安装、声明式命令和 JSON Lines 进程协议；包含查询取消、超时、输出限制和扩展管理。
- 本地扩展需用户明确安装；进程启用 Node 文件权限，禁止直接读写工作区、派生进程、原生插件和 Worker；不提供完整 OS 沙箱，网络仍无系统级隔离。
- 补齐扩展专属数据导出/导入、高风险动作确认、目标路径复制、别名占用项提示、执行超时设置和局部查询状态。
- 增加 Windows/macOS 原生焦点恢复适配、连续 Escape 保护和工作区输入 schema；查询按匹配度排序，空查询按来源分组。

### 官网

- 下载按钮改为上下等宽排列：macOS 在上，Windows 在下；保留原按钮样式与动态下载地址。

## [1.1.0] - 2026-09-07

### 新增

- Windows 10/11 x64 安装包，与 macOS DMG 共用版本及业务代码；新增 Windows 托盘、开机启动、顶部工作区定位及系统加密支持。
- Windows 首页按平台能力隐藏当前窗口和汽水音乐，保留用户组件偏好；剪贴板采用复制后手动 Ctrl+V 粘贴。
- Windows runner 验证安装、真实应用启动、核心 IPC、模拟音视频设备释放、数据保留与卸载；双平台全部通过后统一发布。
- 官网新增「下载 Windows 版本」按钮，与 macOS 下载分别解析最新 Release 对应资产。

### 修复

- 修复恢复工作区备份时自身刷新被导航保护拦截、录音工作区未初始化及便签被旧编辑器覆盖的问题；仍禁止外部导航和打开其他本地文件。

## [1.0.7] - 2026-09-04

### 修复

- 修复首次申请录音或摄像头权限时，macOS 授权弹窗被常驻面板遮挡而无法点击的问题；请求期间临时降低面板层级并激活应用，结束后安全恢复置顶状态。
- 修复摄像头与麦克风权限请求重叠时过早恢复置顶、再次遮挡尚未结束的系统弹窗的问题。
- 修复录音权限等待阶段反复点击不同录音入口会重复发起请求、生成大量错误提示行的问题；等待期间只保留一次启动任务，并显示明确的权限状态。

## [1.0.6] - 2026-09-04

### 新增

- 设置页新增「默认展开页」，可将首页、待办等当前可见 Tab 设为每次唤出面板时的起始页。

### 修复

- 新建待办成功后重置截止时间为当天 23:30，不再沿用上一条待办的手动日期。

## [1.0.5] - 2026-08-31

### 修复

- 修复超长待办提醒被 Node 定时器溢出压缩为 1ms，导致主进程持续循环与 `TimeoutOverflowWarning` 的问题；超过上限的延迟现在分段检查。
- 修复首页组件尺寸切换时的大面积黑块与动画堆积，改为可取消的真实卡片 FLIP 过渡，切换中内容保持可见。
- 减少首页冷启动展开时的重复合成工作，不再同时缩放整个内容层并重播七张卡片的错峰入场。

## [1.0.4] - 2026-08-30

### 修复

- 修复密钥页选中状态无法再次点击取消的问题；取消后批量删除按钮会立即隐藏。
- 修复密钥页批量「删除」按钮错位到搜索框下方的布局问题，选中时保持在搜索框右侧同一行。

## [1.0.3] - 2026-08-30

### 桌面端

- 修复首次启用剪贴板历史时误收录启用前内容，以及文字/图片交替后重复收录同一图片的问题；全新用户目录不再出现预置记录。
- 将剪贴板图片去重改为内容哈希，避免不同图片因尺寸和文件长度相同而被误判。
- 复用 Electron 44 `ClipboardItem` 提供的 PNG 原始字节，并把图片探测降到每 3 秒一次，避免 4K 图片在后台每轮重复编码。
- 修复镜子配图与工作区文件选择器可能被其他应用遮挡的问题，弹窗现在附着主面板且不会触发面板失焦收起。
- 降低展开/收起和首页静置时的 GPU 工作：面板内容层不再参与大面积裁剪，WebGL 动效仅在鼠标悬停时运行。
- 修复多 Tab 分栏时透明容器覆盖整条顶栏、导致展开后无法点击顶部中央空白收起的问题；Tab 点击与 Space 收起保持不变。
- 显式启用渲染沙箱，并拒绝本地窗口导航和创建子窗口。
- 升级至 Electron 44 与 electron-builder 26.15.7，最低系统调整为 macOS 13，并清除完整依赖审计中的已知漏洞。
- 收紧 macOS 运行时 entitlements；启动自检不再在用户尚未同意时触发屏幕捕获 API。
- 通知队列清空后销毁独立通知窗口，避免隐藏 renderer 长期常驻内存。

### 官网

- 新增 Mac 屏幕内的自动 Loading 唤醒动效：等待第二屏截图与首个 TAB 视频就绪，超过 8 秒使用静态封面兜底。
- 将 TAB 演示从 GIF 转换为带 faststart 的无声 H.264 MP4，并使用 WebP 截图作为封面。
- 将第二屏产品截图改为 WebP，并替换为最新的剪贴页面截图。
- 优化 Loading 呼吸幅度、频率、文案尺寸、全屏圆角与壁纸无缝接管效果。

## [1.0.2] - 2026-08-29

### 新增

- 增加常驻面板内的设置页，可配置 API、镜子、功能显示、快捷键、数据目录与开机启动。
- 增加单条密钥删除能力。

### 修复

- 待办截止日期支持跨月选择、自然跨年与正确排序。

## [1.0.1] - 2026-08-28

### 修复

- 在录制页面补齐录音控制，恢复完整的开始、暂停与结束操作。

## [1.0.0] - 2026-08-26

### 发布

- 首个稳定版本，建立固定命名的 Apple Silicon DMG 发布流程。

[未发布]: https://github.com/Simple-Alone/TO-DO-Panel/compare/v1.2.1...HEAD
[1.2.1]: https://github.com/Simple-Alone/TO-DO-Panel/compare/v1.2.0...v1.2.1
[1.2.0]: https://github.com/Simple-Alone/TO-DO-Panel/releases/tag/v1.2.0
[1.1.0]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.7...v1.1.0
[1.0.7]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.6...v1.0.7
[1.0.6]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.5...v1.0.6
[1.0.5]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.4...v1.0.5
[1.0.4]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.3...v1.0.4
[1.0.3]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.2...v1.0.3
[1.0.2]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.1...v1.0.2
[1.0.1]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/xiaopu-ai/TO-DO-Panel/releases/tag/v1.0.0

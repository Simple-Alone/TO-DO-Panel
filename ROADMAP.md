# Dynamic Panel Roadmap

## 当前阶段

- `v1.2.1` 已正式发布；继续处理 macOS 失焦收起时旧展开区域的矩形残影。

## 已完成

- 修复折叠刘海跨显示器移动后仍使用旧菜单栏高度的问题；窗口恢复显示前会同步目标显示器度量。
- 折叠态立即应用目标刘海高度，保留抓握横杠的颜色和悬停动画，避免高度过渡期间横杠被窗口边界裁掉。
- 增加目标显示器度量、跨屏同步顺序及 `24px`／`38px` 抓握横杠位置回归测试。
- 补充三项 macOS 窗口修复的未发布日志，并更新可交付上游的完整修复补丁。
- 修复 Windows Actions 中扩展子进程退出后临时目录短暂锁定导致的测试清理失败；清理使用有限重试并等待服务取消完成。
- 已将三项 macOS 窗口修复归档为 `v1.2.1`，同步版本号、README 下载入口、更新日志与发布说明。
- 修复 macOS Actions 长流程回归测试的时序误报：AI 取消与会话保存／恢复改为等待明确状态，捕获全流程保留逐步断言并增加整体负载余量和超时阶段信息。
- 修复捕获错误处理回归偶发永久等待：精确选择采集窗口，先完成媒体 mock 注入，再异步触发会销毁窗口的拒绝捕获操作，由服务状态断言验证结果。
- 已在根因修复提交 `908c786` 上重建 `v1.2.1` 标签，并发布 macOS Apple Silicon DMG、Windows x64 安装包及各自 SHA-256 校验文件。
- 修复 macOS 透明主窗口失焦收起时保留旧矩形原生阴影的问题；面板继续使用已有 CSS 阴影，Windows 原生阴影行为不变。
- 新增独立 `npm run test:panel-blur` 回归测试，真实执行窗口展开、辅助窗口抢焦、主窗口 `blur`、收起动画及最终 `200 × 38` 尺寸检查；常规测试同时固定平台阴影策略。

## 进行中

- 无。

## 待办

- 在菜单栏高度不同的实体双显示器环境中人工确认跨屏观感。

## 阻塞

- 本机连续运行多个 Electron 测试进程时，既有 `startup.electron.js` 偶发在进入业务阶段前超时；独立失焦专项测试及本次相关单元测试不受影响。

## 最近验证

- 2026-09-24：窗口几何、渲染结构与样式测试共 `78` 项通过。
- 2026-09-24：Electron 渲染交互测试通过，横杠在 `24px` 和 `38px` 折叠高度下均保持底部 `2px` 间距。
- 2026-09-24：完整单元测试 `499` 项通过、`0` 项失败、`3` 项按平台跳过；完整 Electron 流程运行至通知窗口测试时，因已安装的 Dynamic Panel 占用 `127.0.0.1:43821` 而停止。
- 2026-09-24：`fix-macos-build.patch` 已通过当前工作树反向应用检查。
- 2026-09-24：GitHub Actions 运行 `35978805275` 完整通过；macOS 完成 DMG 构建、校验和与 Artifact 上传，Windows 完成测试、安装包构建、安装验证与 Artifact 上传，普通 `main` 提交未发布 Release。
- 2026-09-24：`v1.2.1` 发布策略测试 `6` 项全部通过；模拟 `main` 推送不发布，模拟匹配标签会发布。
- 2026-09-24：`v1.2.1` 首次 `main` Actions 运行 `35980401523` 中，Windows 完整通过；macOS 首次在捕获流程整体 `45s` 上限超时，重跑后暴露 AI 取消固定等待导致的会话时序失败，发布标签暂停。
- 2026-09-24：修复后的启动回归与捕获回归分别在独立临时用户目录完整通过；启动回归因已安装应用占用 `127.0.0.1:43821` 报告通知服务不可用，但不影响本项测试结果。
- 2026-09-24：`main` Actions 运行 `35982487398` 双平台完整通过；`v1.2.1` 标签运行 `35983017914` 的 Windows 任务通过，macOS 捕获回归在 `capture error handling` 阶段超时，未构建 DMG、未创建 Release。
- 2026-09-24：捕获错误处理根因修复后，在两个独立临时用户目录连续完整通过。
- 2026-09-24：根因修复后的 `main` Actions 运行 `35984914968` 完整通过；macOS 完成测试、DMG 构建与校验，Windows 完成测试、安装包构建与安装验证。
- 2026-09-25：删除指向修复前提交 `2b107fe` 的失败标签，在已验证提交 `908c786` 上重建并推送 `v1.2.1`。
- 2026-09-25：正式发布 Actions 运行 `36082326552` 完整通过；macOS 完成测试、DMG 构建与校验，Windows 完成测试、安装包构建与安装验证，发布任务成功创建 Release。
- 2026-09-25：`v1.2.1` Release 为非草稿、非预发布版本，`releases/latest` 已指向该版本；DMG、EXE 及两份 SHA-256 校验文件共四个资产均为 `uploaded` 状态，四个 `releases/latest/download` 地址与 GitHub Pages 官网均返回 HTTP `200`。
- 2026-09-25：修复前独立失焦测试稳定触发真实 `blur`，并在展开边界 `1240 × 616` 的 `closing` 阶段检测到 macOS 原生阴影仍为开启状态；修复后同一流程连续两次通过。
- 2026-09-25：平台策略与渲染结构相关测试 `66` 项全部通过；完整 Node 测试 `505` 项中 `502` 项通过、`3` 项按平台跳过、`0` 项失败，原有刘海和工作区保留 Electron 测试通过。
- 2026-09-25：完整桌面串行测试在既有生产启动测试进入业务阶段前超时；分别使用当前 Node `26.9.0` 与项目要求的 Node `22.13.0` 复现，未将该轮串行测试记为通过。
